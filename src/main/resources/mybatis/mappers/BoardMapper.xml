<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.board_221024.mapper.BoardMapper">
        <insert id="createBoard" parameterType="com.example.board_221024.vo.BoardVo">
            INSERT INTO board(
                               user_id
                             , title
                             , content
                             , board_time
            )VALUES (
                #{userId}
               ,#{title}
               ,#{content}
               , NOW()
                )
        </insert>
</mapper>